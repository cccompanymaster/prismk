# CLAUDE.md — PRISM-K 프로젝트 마스터 지침

이 파일은 Claude Code가 본 프로젝트에서 작업할 때 항상 참고해야 하는 마스터 지침입니다. 모든 작업 시작 전에 이 파일을 먼저 읽어주세요.

---

## 프로젝트 정체성

**PRISM-K** (Personality Reflection through Integrative Scaled Measurement, Korean)는 MBTI를 보완하는 차세대 성격 검사를 위한 온라인 서비스입니다. 이 프로젝트의 목표는 다음 두 가지입니다.

1. **라이트판(36문항, 약 6분)** + **풀 버전(136문항, 약 20~25분)** 두 검사를 운영
2. 결과를 SNS 친화적 카드로 공유 가능하게 만들고, 두 사람의 결과를 비교 분석하는 매칭 기능 제공

**결정적 차별점**: MBTI의 4글자 정체성 매력은 살리되 학술적 한계(이분법, 낮은 신뢰도)를 구조적으로 해소합니다. 16개 패턴 중 메인×서브 조합으로 256가지 정체성 코드(예: `DI-SS`)를 제공합니다.

---

## 핵심 도메인 용어 (혼동 금지)

| 용어 | 의미 |
|---|---|
| **광역 차원 (dimension)** | 6개의 큰 측정 영역: O, C, E, A, ES, HH |
| **하위척도 (facet)** | 차원 안의 4개 세부 측정. 예: O1_상상력, C2_근면성 |
| **보조 차원** | V(맥락 변동성), S(스트레스 반응), G(성장 지향성) |
| **패턴 (pattern)** | 16개 정체성 유형. 예: DI(꿈꾸는 발명가) |
| **코드 (code)** | 메인-서브 패턴 조합. 예: `DI-SS` |
| **시그니처 (signature)** | 각 패턴의 한 단어/컬러/동물 |
| **T-점수** | 평균 50, SD 10인 표준화 점수 |
| **응답 토큰** | 결과를 익명 URL로 공유하기 위한 UUID |

---

## 코딩 원칙

### 언어와 스타일
- **TypeScript** 우선. 모든 새 파일은 .ts/.tsx로 작성.
- 함수는 짧게(50줄 이내), 한 가지 일만.
- 파일은 200줄 이내. 그 이상이면 분리.
- import 순서: 외부 패키지 → 내부 절대 경로 → 상대 경로.

### 명명 규칙
- 컴포넌트: `PascalCase` (예: `ItemCard.tsx`)
- 함수/변수: `camelCase` (예: `calculateScore`)
- 상수: `UPPER_SNAKE_CASE` (예: `LITE_ITEM_COUNT = 36`)
- 타입/인터페이스: `PascalCase` (예: `interface ResponseData`)
- 한국어 표기 변수는 한국어 그대로 (예: `상상력_점수`) 사용 금지 — 영문 변수명만.

### 파일 구조
- `apps/web/` — Next.js 프론트엔드
- `apps/api/` — 백엔드 (NestJS 또는 FastAPI 중 결정)
- `packages/types/` — 공유 타입 정의
- `packages/scoring/` — 채점 알고리즘 (재사용 가능)
- `packages/data/` — 본 패키지의 data/*.json 복사본 (빌드 시 임포트)

### 데이터 흐름
1. 사용자가 검사를 시작하면 백엔드에서 `items.json`을 읽어 셔플 후 응답자에게 전달
2. 응답 완료 시 `scoring` 패키지의 함수로 점수 산출 → DB에 저장 → 토큰 발급
3. `/result/{token}` 페이지에서 토큰으로 결과 조회 → 패턴 매칭 → 결과 카드 생성

---

## 절대 하지 말 것 (DO NOT)

### 1. 학술적 정직성 훼손 금지
- ❌ "당신은 INFP입니다"처럼 단정적 표현 사용 금지
- ✅ "당신은 DI-SS와 가장 가까운 양상을 보입니다"
- ❌ "이 패턴은 안 좋습니다" 같은 부정적 묘사 금지
- ✅ 모든 패턴을 강점과 도전 영역의 균형으로 묘사

### 2. 임상 진단 용어 사용 금지
- ❌ "당신은 우울증 위험이 있습니다"
- ✅ "이 패턴은 추가 평가에서 도움이 될 수 있는 영역을 시사합니다"
- 정서안정성·자기수용 점수가 매우 낮을 때만 위험 신호 안내 자동 노출

### 3. 채용·결혼 결정용으로 포지셔닝 금지
- 모든 결과 페이지·카드 푸터에 "본 결과는 채용·인사·결혼 결정에 단독 사용되지 않습니다" 명시
- 기업·채용 사이트에 결과 노출 시 차단 메커니즘 구현

### 4. 데이터 과다 수집 금지
- 실명·연락처(이메일은 선택)·주소·주민번호 절대 수집 금지
- IP는 마지막 옥텟 마스킹 후 저장
- 무로그인 응답은 기본값. 가입은 선택적

### 5. 라벨 고착화 유도 금지
- 결과지의 첫 페이지와 마지막 페이지에 "이는 현재의 패턴이며 자라납니다" 메시지 필수
- 결과 카드 푸터에도 동일 메시지

### 6. 16개 패턴 균등 노출 깨기 금지
- 마케팅·예시·테스트 데이터에서 특정 패턴이 다른 패턴보다 더 많이 등장하지 않도록
- "인기 패턴" 같은 개념 자체를 만들지 않음

---

## 기술 스택 결정

다음 스택을 표준으로 사용합니다. 변경이 필요하면 PR에서 명시적으로 논의:

```yaml
프론트엔드:
  - Next.js 14 (App Router)
  - TypeScript
  - Tailwind CSS
  - shadcn/ui (Radix 기반)
  - Lucide Icons

백엔드:
  - NestJS (Node.js + TypeScript)  # 기본 선택
  - 또는 FastAPI (Python)            # 데이터 분석 강화 시

데이터베이스:
  - PostgreSQL 15
  - Prisma ORM (NestJS 사용 시)
  - SQLAlchemy (FastAPI 사용 시)

캐시: Redis (Upstash)
인증: NextAuth.js (선택적 가입)
이미지 생성: @vercel/og + Satori
호스팅: Vercel (web) + AWS ECS or Cloudflare Workers (api)
모니터링: Sentry, PostHog
```

---

## 데이터 파일

`data/` 폴더의 JSON 파일들이 본 프로젝트의 핵심 콘텐츠입니다. **데이터 파일은 빌드 시점에 import 또는 fetch**하여 사용하며, **하드코딩 금지**입니다.

- `data/items.json` — 136개 검사 문항. `lite: true` 표시된 36개가 라이트판
- `data/patterns.json` — 16개 패턴 + 시그니처 + 관계 매트릭스 + 채점 규칙

---

## 작업 순서

`TASKS.md` 파일을 참고하세요. 8개 Phase로 분할되어 있으며, 각 Phase는 의존 관계가 명시되어 있습니다. 항상 Phase 순서대로 진행하되, Phase 2와 Phase 3은 병렬 가능합니다.

각 task 완료 시:
1. 명시된 acceptance criteria 모두 충족 확인
2. 테스트 작성 또는 검증 절차 수행
3. README 또는 인라인 주석 업데이트

---

## 막혔을 때

- 비즈니스 로직 결정이 필요한 경우 → `PROJECT_SPEC.md` 참고
- 도메인 용어가 헷갈릴 때 → 본 파일의 "핵심 도메인 용어" 표 참고
- 채점 알고리즘 구현이 헷갈릴 때 → `data/patterns.json`의 `scoring` 객체 참고
- 그래도 막히면 작업을 멈추고 사람에게 질문 (가정으로 진행 금지)

---

## 마지막으로

이 프로젝트는 학술적 우수성과 대중적 매력의 균형이라는 어려운 줄타기를 합니다. 매력을 위해 정직성을 깎지 않고, 정직성을 위해 매력을 버리지 않습니다. 모든 의사결정에서 이 두 축을 함께 고려해주세요.

**"검사가 사람을 정형화하지 않을 때, 검사는 자기이해의 도구가 된다."**
